namespace FabBooks

open Xamarin.Forms

module Colors =
    let backgroundPrimary = Color.FromRgb(237, 255, 255)
    let backgroundSecondary = Color.FromRgb(213, 247, 247)

    let textPrimary = Color.FromRgb(30, 30, 30)

    let accentPrimaryLight = Color.FromRgb(110, 220, 220)
    
    let success = Color.Green
    let loading = Color.Yellow
    let failure = Color.Red
